includes: ['layer:basic', 'interface:mysql']  # if you use any interfaces, add them here
options:
  basic:
    packages:
        - python-yaml
        - python-bottle
        - python-mysqldb
        - python-jsonschema
        - python-paramiko
        - python-argcomplete
        - python-requests

        # These are for openstack as a VIM
        - python-novaclient
        - python-keystoneclient
        - python-glanceclient
        - python-neutronclient

        # mysql client needed to install database
        - mariadb-client-core-5.5
